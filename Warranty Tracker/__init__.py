#Here we import all of the files needed
#We need the files that aren't in __init__.py or __manifest__.py os in this case model.py
#What about the views? Do we need to add them in here?
import model

